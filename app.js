const $=s=>document.querySelector(s);const pad=n=>String(n).padStart(2,'0');
function dateTool(){const start=$('#start'),end=$('#end'),out=$('#date-result');const today=new Date(),week=d=>[0,6].includes(d.getDay());start.value=`${today.getFullYear()}-${pad(today.getMonth()+1)}-${pad(today.getDate())}`;end.value=`${today.getFullYear()}-${pad(today.getMonth()+1)}-${pad(today.getDate()+30)}`;$('#calc-date').onclick=()=>{let a=new Date(start.value+'T00:00:00'),b=new Date(end.value+'T00:00:00');if(!start.value||!end.value||b<a){out.textContent='请选择有效的起止日期。';return}let days=Math.round((b-a)/864e5),work=0;for(let d=new Date(a);d<=b;d.setDate(d.getDate()+1))if(!week(d))work++;out.textContent=`相差 ${days} 天（含首尾共 ${days+1} 天）\n期间工作日：${work} 天；周末：${days+1-work} 天。`}}
function textTool(){const input=$('#text-input'),out=$('#text-result');const run=()=>{let x=input.value,chars=x.replace(/\s/g,'').length,words=(x.trim().match(/[A-Za-z0-9]+(?:['’-][A-Za-z0-9]+)*/g)||[]).length,cn=(x.match(/[\u4e00-\u9fff]/g)||[]).length,lines=x?x.split(/\r?\n/).length:0;out.textContent=`字符（不含空格）：${chars}\n汉字：${cn}\n英文单词：${words}\n行数：${lines}`};input.oninput=run;$('#clean-space').onclick=()=>{input.value=input.value.replace(/[ \t]+/g,' ').replace(/ *\n */g,'\n').trim();run()};run()}
function jsonTool(){const i=$('#json-input'),o=$('#json-result');$('#format-json').onclick=()=>{try{o.textContent=JSON.stringify(JSON.parse(i.value),null,2)}catch(e){o.textContent='格式错误：'+e.message}};$('#minify-json').onclick=()=>{try{o.textContent=JSON.stringify(JSON.parse(i.value))}catch(e){o.textContent='格式错误：'+e.message}}}
function passwordTool(){const out=$('#password-result'),len=$('#length');const make=()=>{let chars='ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%&*?',n=Math.min(64,Math.max(8,Number(len.value)||16)),a=new Uint32Array(n);crypto.getRandomValues(a);out.textContent=[...a].map(v=>chars[v%chars.length]).join('')};$('#make-password').onclick=make;$('#copy-password').onclick=()=>navigator.clipboard.writeText(out.textContent);make()}
document.addEventListener('DOMContentLoaded',()=>({date:dateTool,text:textTool,json:jsonTool,password:passwordTool})[document.body.dataset.tool]?.());

